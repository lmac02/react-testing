import { RequestMethod } from 'node-mocks-http';
import { Context } from 'koa';
import { MockCookies } from '../create-mock-cookies';
export interface Dictionary<T> {
    [key: string]: T;
}
export interface MockContext extends Context {
    cookies: MockCookies;
    request: Context['request'] & {
        body?: any;
        session?: any;
    };
}
export interface Options<CustomProperties extends Object, RequestBody = undefined> {
    url?: string;
    method?: RequestMethod;
    statusCode?: number;
    session?: Dictionary<any>;
    headers?: Dictionary<string>;
    cookies?: Dictionary<string>;
    state?: Dictionary<any>;
    encrypted?: boolean;
    host?: string;
    requestBody?: RequestBody;
    throw?: Function;
    redirect?: Function;
    customProperties?: CustomProperties;
}
export default function createContext<CustomProperties, RequestBody = undefined>(options?: Options<CustomProperties, RequestBody>): MockContext & CustomProperties;
